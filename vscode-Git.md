# 基础操作
Vscode内置的Git功能非常强大，可以方便地进行版本控制。以下是Git菜单的使用方法：

#### 1. 拉取（Pull）
- **含义**：从远程仓库下载**最新代码**，并**自动合并**到你当前的分支。
- **相当于**：`git pull`（= fetch + merge）
---

#### 2. 推送（Push）
- **含义**：把你本地**已提交**的更改，上传到远程仓库。
- **相当于**：`git push`
---
#### 3. 抓取（Fetch）
- **含义**：只从远程仓库**下载**最新信息（分支、提交记录），但**不合并**到你的工作区。
- **相当于**：`git fetch`
- **使用场景**：你想看看远程有没有新分支或新提交，但又不想立刻影响当前代码，就用“抓取”先看一眼。
---
#### 4. 提交（Commit）
- **含义**：把你**暂存区**（Staged）的更改，保存为一个本地版本快照，并写上说明信息。
- **相当于**：`git commit -m "消息"`

下面会有一些子选项，这里分别说明一下：
##### 4.1 提交
- **含义**：只提交**你已经手动暂存**（点击了文件旁的 `+` 号）的那些文件。


##### 4.2 提交已暂存文件（Commit Staged）
- **含义**：和上面**完全一样**，只是名称更明确。


##### 4.3 全部提交（Commit All）
- **含义**：**自动暂存所有已修改的文件**（相当于 `git add .`），然后一次性全部提交。
- **适用场景**：你修改了多个文件，确信它们都属于同一个功能，懒得一个个点暂存。

##### 4.4 撤消上次提交（Undo Last Commit）
- **含义**：回退最近一次提交，但**保留所有修改**在工作区（相当于 `git reset --soft HEAD~1`）。
- **适用场景**：提交后发现写错了提交信息，或者想重新拆分提交。

##### 4.5 中止变基（Abort Rebase）
- **含义**：如果你正在进行 `rebase`（变基）操作遇到了冲突，点击这个可以**完全取消**本次变基，回到变基前的状态。
- **相当于**：`git rebase --abort`


##### 4.6 提交（修改）[Commit (Amend)]
- **含义**：把当前暂存的文件，**合并到上一次提交中**，并可以修改提交信息。
- **相当于**：`git commit --amend`
- **适用场景**：你刚提交完，发现漏了一个小修改，不想创建新的提交记录，直接修改上一个提交。用这个功能把漏改的加进去，历史记录还是只有一次提交。

##### 4.7 提交已暂存文件（Commit Staged）
- **含义**：和上面**完全一样**，只是名称更明确。因为 Git 的提交本身就是提交暂存区的内容。

##### 4.8 全部提交（修改）[Commit All (Amend)]
- **含义**：自动暂存所有修改，然后**覆盖上一次提交**。
- **适用场景**：提交后立即发现整个功能有多个文件需要调整，想全部包含在上一个提交里。
- **⚠️ 注意**：如果已经推送到远程，**不要用 amend**，否则会改变历史，导致队友冲突。


##### 4.9 提交（已签收）[Commit (Signed-off-by)]
- **含义**：提交暂存的文件，并在提交信息末尾附上签名。
- **相当于**：`git commit -s`
- **适用场景**：参与开源项目，要求每次提交必须有签名认证。

##### 4.10 提交已暂存文件（已署名）[Commit Staged (Signed-off-by)]
- **含义**：和上面相同，只是名称表述差异。


##### 4.11 全部提交（已署名）[Commit All (Signed-off-by)]
- **含义**：全部暂存 + 提交 + 签名。
---


#### 5. 更改（Changes）
#### 5.1 暂存所有更改（Stage All Changes）
- **含义**：将**所有已修改和新增的文件**一次性添加到暂存区（Staged），准备提交。
- **相当于**：`git add .` 或 `git add -A`
- **适用场景**：
  - 你修改了多个文件，它们都属于同一个功能或修复，想一次性全部提交
  - 不想逐个点击文件旁的 `+` 号，省时间

#### 5.2 取消暂存所有更改（Unstage All Changes）
- **含义**：将**暂存区中的所有文件**一次性移出暂存区，回到未暂存状态（但**保留文件内容**，不会丢失修改）。
- **相当于**：`git reset HEAD .` 或 `git reset`（不带参数）
- **适用场景**：
  - 你误操作了"暂存所有更改"，想撤回重新选择
  - 发现暂存了不该提交的文件，想重新分批提交

#### 5.3 放弃所有更改（Discard All Changes）

- **含义**：**丢弃所有未暂存的修改**，将工作区恢复到上次提交的状态。**这是一个危险操作！**
- **相当于**：`git checkout -- .` 或 `git restore .`
- **适用场景**：
  - 你做了大量实验性修改，但搞砸了，想回到干净状态重来
  - 想彻底放弃所有未提交的改动
- **例子**：你改了 10 个文件，发现方向完全错了，想回到上一次提交的干净状态，点"放弃所有更改" → 所有修改**永久丢失**，无法恢复。
- **⚠️ 注意**：
  - 此操作**不可逆**！没有确认对话框或只有一次确认
  - **不会影响已暂存的文件**（只丢弃"更改"区域的文件）
  - 如果你有已暂存的文件，它们不受影响

---
#### 6. 拉取、推送
##### 6.1 同步（Sync）
- **含义**：**一次完成"拉取"+"推送"** 两个操作。先拉取远程最新代码到本地，再把你本地的提交推送到远程。
- **相当于**：`git pull` + `git push`（如果本地有新提交）
- **适用场景**：
  - 日常开发中最常用的操作，一键同步团队最新代码并推送自己的更改
- **⚠️ 注意**：
  - 如果有冲突，同步会失败，需要手动解决
  - 同步执行的是**合并（merge）**，不是变基（rebase）


##### 6.2 拉取（Pull）
- **含义**：从远程仓库下载最新代码，并**自动合并（merge）** 到你当前分支。
- **相当于**：`git pull`（= `git fetch` + `git merge`）
- **适用场景**：你想获取团队最新代码，并用**合并**的方式整合到你的分支

##### 6.3 拉取（变基）[Pull (Rebase)]
- **含义**：从远程仓库下载最新代码，但用**变基（rebase）** 而不是合并来整合。
- **相当于**：`git pull --rebase`（= `git fetch` + `git rebase`）
- **适用场景**：
  - 你想保持线性历史，避免产生合并提交
  - 你的本地提交还没推送，想整理成一条直线
- **例子**：你有本地提交 A、B，远程有新提交 C，执行"拉取（变基）"后，A、B 会被"挪到"C 后面，变成 C → A' → B'
- **对比**：
  - `拉取（merge）`：历史有分叉 + 合并提交
  - `拉取（变基）`：历史是一条直线
- **⚠️ 注意**：如果变基过程中有冲突，需要逐个解决

##### 6.4 拉取自...（Pull from...）
- **含义**：从指定的**远程仓库**或**分支**拉取代码，不一定是当前分支的默认上游。
- **相当于**：`git pull <远程仓库名> <分支名>`
- **适用场景**：
  - 从同事的远程分支拉取代码（如 `git pull origin feature/login`）
  - 从另一个远程仓库拉取（如备份仓库）
- **例子**：你想从同事的 `feature/payment` 分支拉取代码审查，点击"拉取自..." → 选择 `origin/feature/payment`

##### 6.5 推送（Push）
- **含义**：将你本地的提交上传到远程仓库的**对应分支**。
- **相当于**：`git push`
- **适用场景**：提交完成后，让团队其他人看到你的改动

##### 6.6 推送到（Push to）
- **含义**：推送到指定的远程仓库或分支，不一定是默认上游。
- **相当于**：`git push <远程仓库名> <本地分支名>:<远程分支名>`
- **适用场景**：
  - 推送到不同的远程仓库（如同时推送到 GitHub 和 GitLab）
  - 推送到不同名的分支
- **例子**：你想把本地的 `feature/login` 推送到远程的 `experimental/login`，点击"推送到..." → 配置目标分支

##### 6.7 抓取（Fetch）
- **含义**：只从远程仓库**下载**最新的提交和分支信息，但**不合并**到你的工作区。
- **相当于**：`git fetch`
- **适用场景**：
  - 只想看看远程有没有新内容，但不想立刻影响当前代码
  - 查看同事新建了哪些分支


##### 6.8 抓取（删除）[Fetch (Delete)]
- **含义**：抓取远程最新信息的同时，**清理本地不再跟踪的远程分支引用**。
- **相当于**：`git fetch --prune` 或 `git remote prune origin`
- **适用场景**：
  - 远程分支已被删除（如功能合并后清理），但本地还残留 `origin/old-feature` 的引用
  - 想让本地远程分支列表保持干净
- **例子**：同事删除了远程的 `feature/old`，你执行"抓取（删除）"，本地的 `origin/feature/old` 引用也被清除
- **好处**：分支列表不再显示已删除的远程分支，更整洁

##### 6.9 从所有远程存储库中抓取（Fetch from All Remotes）
- **含义**：如果你的项目配置了**多个远程仓库**（如 origin、backup、upstream），一次性从**所有**远程仓库抓取更新。
- **相当于**：`git fetch --all`
- **适用场景**：
  - 你配置了多个远程仓库（比如同时从 GitHub 和公司内部 Git 服务器同步）
  - 想一次性获取所有远程的最新信息
- **例子**：
  ```
  远程仓库：
  - origin    → GitHub（主要）
  - upstream  → 上游开源项目
  - backup    → 备份服务器
  ```
  点击"从所有远程存储库中抓取"，三个仓库的更新一次性下载
---
#### 7. 分支
##### 7.1 合并（Merge）
- **含义**：将**其他分支**的更改**合并**到当前分支。
- **相当于**：`git merge <分支名>`
- **适用场景**：
  - 功能开发完成，把 `feature` 分支合并到 `main` 分支
  - 同步其他人的最新改动到你的分支
- **结果**：产生一个**合并提交**（Merge Commit），历史会保留分叉


##### 7.2 变基（Rebase）
- **含义**：找到当前分支和目标分支的**共同祖先**，然后把当前分支从共同祖先之后的所有提交，**逐个复制**到目标分支的最新提交后面，生成新的提交（内容相同，哈希值不同），并更新当前分支指向这些新提交。**目标分支本身不受任何影响**。
- **相当于**：`git rebase <目标分支>`
- **适用场景**：
  - 让你的功能分支包含主分支的最新更新
  - 保持线性历史
- **例子**：你正在 `feature/login` 分支开发登录功能，已经提交了 3 次（D、E、F）。与此同时，同事在 `main` 分支上推送了 2 个新提交（C1、C2）。现在你想让 `feature/login` 包含 `main` 的最新代码，于是执行变基。

##### 7.3 创建分支（Create Branch）
- **含义**：基于**当前分支的当前状态**创建一个新分支。
- **相当于**：`git branch <新分支名>`（创建但不切换）
- **适用场景**：
  - 开始开发新功能
  - 修复 Bug
  - 尝试新的实现方案
- **例子**：当前在 `main` 分支，点击"创建分支..." → 输入 `feature/payment` → 创建了一个新分支


##### 7.4 从现有来源创建新的分支（Create Branch from）
- **含义**：从**指定的分支、标签或提交**创建新分支，不一定是当前分支。
- **相当于**：`git branch <新分支名> <来源>`
- **适用场景**：
  - 从某个旧版本（标签）创建修复分支
  - 从同事的分支创建自己的分支
  - 从某个历史提交创建实验分支


##### 7.5 重命名分支（Rename Branch）
- **含义**：修改当前分支的名称（**本地分支**）。
- **相当于**：`git branch -m <新名称>`
- **适用场景**：
  - 分支名拼写错误
  - 功能范围变化，需要改名
  - 规范分支命名
- **例子**：`feature/login` 想改成 `feature/user-auth` → 点击"重命名分支..." → 输入新名称
- **⚠️ 注意**：
  - 如果已推送到远程，本地改名后需要：
    1. 删除远程旧分支
    2. 推送新分支
    3. 重新设置跟踪关系

##### 7.6 删除分支（Delete Branch）
- **含义**：删除**本地分支**（当前分支不能被删除）。
- **相当于**：`git branch -d <分支名>` 或 `-D`（强制）
- **适用场景**：
  - 功能已合并，删除本地分支保持整洁
  - 废弃的实验性分支
- **例子**：`feature/login` 已合并到 `main`，点击"删除分支..." → 选择 `feature/login`
- **⚠️ 注意**：
  - 未合并的分支会提示无法删除（除非用强制删除）
  - 删除前确保更改已合并或不再需要

##### 7.7 删除远程分支...（Delete Remote Branch...）
- **含义**：删除**远程仓库**上的分支（如 GitHub 上的分支）。
- **相当于**：`git push origin --delete <分支名>`
- **适用场景**：
  - 功能已合并到主分支，清理远程仓库
  - 误推送的分支需要移除
- **例子**：`origin/feature/login` 已合并，点击"删除远程分支..." → 选择 `origin/feature/login`
- **⚠️ 注意**：删除后无法恢复（除非有人本地还有该分支）


##### 7.8 发布分支（Publish Branch）
- **含义**：将**本地分支**推送到远程仓库，并建立跟踪关系。
- **相当于**：`git push -u origin <分支名>`
- **适用场景**：
  - 第一次推送新创建的功能分支
  - 让团队其他人能看到你的分支
- **例子**：本地创建了 `feature/payment`，想推送到 GitHub 让同事协作 → 点击"发布分支..."
---
#### 8. 远程
##### 8.1 添加远程存储库（Add Remote）
- **含义**：为当前 Git 仓库**添加一个新的远程仓库地址**，并给它起一个名字（如 `origin`、`backup`、`upstream` 等）。
- **相当于**：`git remote add <名称> <URL>`
- **操作步骤**：
  1. 点击"添加远程存储库..."
  2. 输入**远程仓库名称**（如 `backup`）
  3. 输入**远程仓库 URL**（如 `https://github.com/用户名/仓库名.git`）
  4. 点击确定，完成添加

- **适用场景**：
  - 你的项目需要关联**多个远程仓库**
  - 比如：同时推送代码到 GitHub 和 Gitee（码云）
  - 参与开源项目：`origin` 指向你的 fork，`upstream` 指向原项目
  - 添加团队内部的备用仓库


##### 8.2 删除远程存储库（Remove Remote）
- **含义**：从当前 Git 仓库的配置中**移除一个已存在的远程仓库关联**。
- **相当于**：`git remote remove <名称>` 或 `git remote rm <名称>`
- **操作步骤**：
  1. 点击"删除远程存储库..."
  2. 从下拉列表中选择要删除的远程仓库名称（如 `backup`）
  3. 确认删除

- **适用场景**：
  - 某个远程仓库不再需要了（如备份服务器停用）
  - 远程仓库地址变更，需要删除旧的重新添加
  - 清理不再使用的远程引用
  - 误添加了多余的远程仓库

- **⚠️ 注意**：
  - **只删除本地配置**，不会影响远程仓库本身
  - 删除后，本地分支的跟踪关系如果依赖该远程，需要重新设置
  - 不可直接恢复（需要重新 `git remote add`）

---


#### 9. 存储
- **“存储”**（Stash）功能是 Git 中非常实用的“临时保存”功能，让你可以把当前未提交的修改暂时“藏”起来，让工作区恢复干净，方便切换分支或拉取代码。

##### 9.1 储藏（Stash）
- **含义**：将当前**所有已跟踪文件的修改**（包括已暂存和未暂存的）保存到储藏栈，工作区恢复到干净状态。
- **相当于**：`git stash push`（即 `git stash`）
- **适用场景**：
  - 你正在开发功能，突然需要紧急修复 Bug，但又不想提交半成品代码
  - 需要切换分支，但当前修改还没完成
- **例子**：你在 `feature/login` 分支修改了 3 个文件，突然收到紧急线上 Bug，点击“储藏”，工作区变干净，切换分支修 Bug，修完回来“弹出储藏”继续工作


##### 9.2 储藏（包含未跟踪）[Stash (Include Untracked)]
- **含义**：储藏**所有文件**，包括**新建的、尚未被 Git 跟踪的文件**（即 `git status` 中显示为 `Untracked` 的文件）。
- **相当于**：`git stash push --include-untracked` 或 `git stash -u`
- **适用场景**：
  - 你创建了一些新文件（如配置文件、日志文件），还没 `git add`，它们不在储藏范围内，但你也想一并保存
- **例子**：你新建了 `temp.log` 和 `config.local.js`（都是未跟踪文件），点击“储藏（包含未跟踪）”，这些新文件也会被存起来
- **对比**：
  - `储藏`：只保存已跟踪的文件
  - `储藏（包含未跟踪）`：保存所有文件（包括新建的）

##### 9.3 储藏暂存 [Stash Staged]
- **含义**：只储藏**已暂存（Staged）**的文件，不储藏工作区未暂存的修改。
- **相当于**：`git stash push --staged`
- **适用场景**：
  - 你已经 `git add` 了一些文件准备提交，但发现需要先处理其他事情，只想保存这些已暂存的文件
- **例子**：你暂存了 `index.html` 和 `style.css`，但 `app.js` 的修改还没想好，点击“储藏暂存”，只有 `index.html` 和 `style.css` 被储藏，`app.js` 的修改还留在工作区

##### 9.4 应用最新储藏（Apply Latest Stash）
- **含义**：将**栈顶的最新储藏**（stash@{0}）应用到当前工作区，但**不从栈中删除**该储藏记录。
- **相当于**：`git stash apply`
- **适用场景**：
  - 你想恢复最新的储藏内容，但希望保留该储藏记录（可能以后还要用）
  - 想先看看恢复效果，确定没问题再删除

##### 9.5 应用储藏...（Apply Stash...）
- **含义**：从储藏的**列表中选择一个**（不一定是最新的）应用到当前工作区，**不从栈中删除**该记录。
- **相当于**：`git stash apply stash@{n}`
- **适用场景**：
  - 你想恢复**特定**的储藏（不是最新那个）
  - 比如你储藏了 3 次，想恢复第 2 个
- **例子**：你的储藏列表有 `stash@{0}`（最新的）、`stash@{1}`（昨天的）、`stash@{2}`（上周的），点击“应用储藏...” → 选择 `stash@{1}`，应用昨天的储藏

##### 9.6 弹出最新储藏（Pop Latest Stash）
- **含义**：将**栈顶的最新储藏**（stash@{0}）应用到当前工作区，并**从栈中删除**该储藏记录。
- **相当于**：`git stash pop`
- **适用场景**：
  - 你确定要恢复最新的储藏，并且不再需要这个储藏记录了（最常见用法）
  - 日常恢复储藏的首选操作
- **例子**：你储藏了代码，处理完其他事情，想继续工作，点击“弹出最新储藏”，代码恢复，储藏记录被自动清理

##### 9.7 弹出储藏...（Pop Stash...）
- **含义**：从储藏的**列表中选择一个**应用到当前工作区，并**从栈中删除**该记录。
- **相当于**：`git stash pop stash@{n}`
- **适用场景**：
  - 你想恢复**特定**的储藏，并且确定不再需要该记录
- **例子**：你想恢复 `stash@{1}`，并且之后不需要了，点击“弹出储藏...” → 选择 `stash@{1}`

##### 9.8 删除储藏...（Drop Stash...）
- **含义**：从储藏栈中**删除指定的一个储藏**，但不应用它。
- **相当于**：`git stash drop stash@{n}`
- **适用场景**：
  - 某个储藏已经过时了，不再需要
  - 你“应用”了某个储藏并确认没问题，想手动清理
  - 储藏内容已经不需要了，想清理栈

##### 9.9 删除所有储藏...（Drop All Stashes...）
- **含义**：**一次性删除所有的储藏记录**，清空整个储藏栈。
- **相当于**：`git stash clear`
- **适用场景**：
  - 你的储藏栈里积累了太多旧记录，想一次性全部清理
  - 项目完成，不再需要任何储藏
- **⚠️ 注意**：此操作**不可逆**，所有储藏永久丢失

##### 9.10 查看储藏条目...（View Stash Entry...）
- **含义**：查看**指定储藏**的详细信息（包含哪些文件、修改内容等）。
- **相当于**：`git stash show stash@{n}` 或 `git show stash@{n}`
- **适用场景**：
  - 你不确定某个储藏里到底存了什么，想预览一下
  - 在“应用”或“弹出”之前，确认内容是否正确
  - 想对比不同储藏的差异
- **例子**：你的储藏列表有 3 条，记不清 `stash@{1}` 存了什么，点击“查看储藏条目...” → 选择 `stash@{1}`，VS Code 会显示该储藏包含的文件列表和修改内容

---
#### 10. 标记

-  **“标记”（Tags）** 是 Git 中用来给某个特定的提交（commit）打上一个**有意义的名称**,就像给一本书的某个版本贴上书签，方便以后快速找到
- **最常见的用途**：标记软件的发布版本号

##### 10.1 创建标记...（Create Tag...）

- **含义**：为当前分支的**最新提交**创建一个新标签。
- **相当于**：`git tag <标签名>`（轻量）或 `git tag -a <标签名> -m "注释"`（附注）
- **操作步骤**：
  1. 点击"创建标记..."
  2. 输入标签名称（如 `v1.0.0`）
  3. （可选）输入注释信息
  4. 点击确定

- **适用场景**：
  - 软件发布正式版本（如 v1.0.0、v2.1.3）
  - 标记重要的里程碑
  - 标记某个稳定的可发布版本
  - 标记 Beta/RC 版本（如 v1.0.0-beta.1）

- **命名规范建议**：
  - ✅ `v1.0.0`、`v1.2.3`、`v2.0.0-beta.1`
  - ✅ `release-2024-01-01`
  - ❌ `test`、`asdf`、`最新版本`（不够明确）


##### 10.2 删除标签...（Delete Tag...）

- **含义**：删除**本地**的一个标签。
- **相当于**：`git tag -d <标签名>`
- **操作步骤**：
  1. 点击"删除标签..."
  2. 从下拉列表中选择要删除的标签
  3. 确认删除

- **适用场景**：
  - 标签名打错了（如把 v1.0.0 打成了 v1.0.O）
  - 标签对应的版本有严重 Bug，需要重新打标签
  - 清理不再需要的本地标签
  - 本地标签和远程不同步，想重新同步

- **⚠️ 注意**：
  - 只删除**本地**标签，不影响远程
  - 如果标签已推送到远程，需要额外删除远程标签


##### 10.3 删除远程标记...（Delete Remote Tag...）

- **含义**：删除**远程仓库**上的一个标签。
- **相当于**：`git push origin --delete <标签名>` 或 `git push origin :<标签名>`

- **适用场景**：
  - 错误地推送了标签到远程，需要撤回
  - 发布的版本有严重问题，需要撤回该版本标签
  - 清理远程仓库中不再使用的旧标签

- **⚠️ 注意**：
  - 如果其他人已经基于该标签工作，删除会造成混乱
  - 正式发布的版本标签尽量不要删除
  - 删除后无法直接恢复（除非重新推送）


##### 10.4 推送标记（Push Tags）

- **含义**：将**本地所有标签**推送到远程仓库。
- **相当于**：`git push --tags`
- **适用场景**：
  - 你创建了本地标签（如 v1.0.0），想让团队其他人也能看到
  - 发布版本后，同步标签到 GitHub/GitLab
  - 一次性推送所有本地标签

- **⚠️ 注意**：
  - 推送标签**不会**自动推送代码（需要单独推送 commit）
  - 默认 `git push` 不会推送标签，必须显式推送

- **例子**：
  ```
  你完成了 v1.0.0 版本
  在本地创建了标签 v1.0.0
  
  点击"推送标记" → 标签出现在 GitHub 的 Releases 区域 ✅
  团队成员可以看到这个版本标签
  ```

---
#### 11. 显示GIT输出
- **含义**：在 VS Code 中打开一个专门的面板，显示 VS Code 在后台执行的每一条 Git 命令及其输出结果。**是排查 Git 问题的第一工具**。